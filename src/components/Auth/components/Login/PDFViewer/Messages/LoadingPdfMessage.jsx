import { Box } from '@mui/material';

const LoadingPdfMessage = () => {
  return (
    <Box sx={{ textAlign: 'center' }}>
      <p>Cargando PDF...</p>
    </Box>
  );
};

export default LoadingPdfMessage;
